<%= form_for(@category) do |f| %>
  <% if @category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
      <% @category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


<table>
	<tr><td><%= f.label :name %></td><td><%= f.text_field :name %></td></tr>
	<tr><td><%= f.label :parent_id %></td><td><%= select "category", "parent_id",Category.where("parent_id = 0").all.collect {|c|[c.name,c.id]},{:include_blank => true} %></td></tr>
	<tr><td><%= f.submit %></td><td></td></tr>
	
	
</table>
  <div class="actions">
    
  </div>
<% end %>
